# See shop-operator/helm.yaml in this same clusters/local/ directory for why this is a local
# path instead of a real chart registry reference.
chart: ../../../shophub-helm-charts/charts/shophub
version: 0.1.0
